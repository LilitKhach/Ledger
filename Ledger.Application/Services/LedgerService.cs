using Ledger.Application.ViewModels;

namespace Ledger.Application.Services
{
    public class LedgerService
    {
        public LedgerViewModel Deposit(decimal amount)
        {
            return LedgerViewModel
        }
    }
}
